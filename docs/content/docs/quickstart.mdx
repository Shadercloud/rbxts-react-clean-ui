---

title: Quick Start
description: Start using React Clean UI right now
icon: Rocket
---

## Prerequisite

This package is intended to be used within a [Roblox-ts](https://roblox-ts.com/) (@rbxts) project.

First you should setup a roblox-ts project using `npm init roblox-ts`  ([read more about roblox-ts](https://roblox-ts.com/docs/quick-start))

Then install **React Clean UI** within your roblox-ts project.

## Installation

<del>
    ```bash
    npm install @rbxts/react-clean-ui
    ```
</del>

<Callout type="warn" title="Development status">
    React Clean UI is currently under active development and has not yet been published to NPM. APIs, component props, and installation details may change before the first stable public release.
</Callout>

Until the package is published, you can clone the repository into your project or install it directly from GitHub.

```bash
npm install github:Shadercloud/rbxts-react-clean-ui
```

### React Installation

If this is a new project and you have not yet configured React, then open `default.project.json` in your project root.

Find this section:
```json
"@rbxts": {
    "$path": "node_modules/@rbxts"
},
```

Then add the `@rbxts-js` path below it so that it looks like this:
```json
"@rbxts": {
    "$path": "node_modules/@rbxts"
},
"@rbxts-js": {
    "$path": "node_modules/@rbxts-js"
},
```


## Basic usage

Import components directly from the package:

```tsx
import React from "@rbxts/react";
import {
    Box,
    Button,
    Container,
    HStack,
    Text,
    VStack,
} from "@rbxts/react-clean-ui";
export function WelcomePanel() {
    return (
        <Container width="100%" height="100%" center="50%">
            <Box spacing="lg">
                <VStack>
                    <Text text="Welcome" variant="title" />
                    <Text
                        text="Build consistent Roblox interfaces from reusable components."
                        variant="body"
                    />
                    <HStack>
                        <Button text="Continue" intent="primary" />
                        <Button text="Cancel" />
                    </HStack>
                </VStack>
            </Box>
        </Container>
    );
}
```
