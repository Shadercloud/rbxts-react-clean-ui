# out/ is generated by `npm run build` (rbxtsc) and always written with LF
# line endings. Pin it to eol=lf so it's committed the same way regardless
# of a contributor's core.autocrlf setting, avoiding false diffs after
# every build.
out/** text eol=lf
